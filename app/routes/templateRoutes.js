module.exports = function(router) {

	// ROUTES FOR OUR course API
	// =============================================================================
	
	// var template = require('../controllers/template'),
	//     auth  = require('../../config/auth');

	// // on routes that end in /course
	// // ----------------------------------------------------
	// router.route('/courses')

	// 	// create a room (accessed at POST http://localhost:8080/api/rooms)
	// 	.post(course.create)

	// 	// get all the rooms (accessed at GET http://localhost:8080/api/rooms)
	// 	.get(course.all);

	// // on routes that end in /course/:course_id
	// // ----------------------------------------------------
	// router.route('/courses/:course_id')

	// 	// get the course with that id
	// 	.get(auth.ensureAuthenticated, course.get)

	// 	// update the course with this id
	// 	.put(auth.ensureAuthenticated, course.update)

	// 	// delete the course with this id 
	// 	.delete(course.deleteCourse);

};